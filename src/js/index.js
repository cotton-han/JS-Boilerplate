import { sum } from '@/js/sum'

const x = 5
console.log(x)

const add = (a, b)=> a + b
add(1, 2)

const array = Array.from([1, 2, 3])
Object.assign({}, {})
console.log(array)

document.body.innerText = sum(1, 2)
